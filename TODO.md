- [ ] Implement Feedback Widget in src/App.jsx using functional components and useState with state {good, neutral, bad}
- [ ] Add helper methods countTotalFeedback() and countPositiveFeedbackPercentage() based on state
- [ ] Refactor UI into components: Section, Statistics, FeedbackOptions, Notification
- [ ] Render Statistics only when at least one feedback exists; otherwise render Notification("There is no feedback")
- [x] Verify app builds and works (npm run build / npm run dev)

- [ ] Deploy to GitHub Pages and push code to repository
